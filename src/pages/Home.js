import React from 'react';
import { logo } from '../assets/images/images';

const Home = () => {
    return (
        <div>
            <img src={logo} alt="" />
        </div>
    );
}

export default Home;
